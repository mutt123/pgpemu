# pgpemu-esp32/main/CMakeLists.txt
# WiFi AP Integration - Corrected Version

idf_component_register(
    SRCS
        "button_input.c"
        "config_secrets.c"
        "config_storage.c"
        "log_tags.c"
        "mutex_helpers.c"
        "nvs_helper.c"
        "pgp_autobutton.c"
        "pgp_autosetting.c"
        "pgp_bluetooth.c"
        "pgp_cert.c"
        "pgp_gap.c"
        "pgp_gatts.c"
        "pgp_handshake.c"
        "pgp_handshake_multi.c"
        "pgp_led_handler.c"
        "pgpemu.c"
        "settings.c"
        "stats.c"
        "uart.c"
        
        # WiFi AP Feature
        "wifi_ap_manager.c"
        "web_server.c"
        "button_wifi_trigger.c"
        
    INCLUDE_DIRS "."
    
    REQUIRES
        bt
        driver
        esp_http_server
        esp_timer
        esp_wifi
        freertos
        json
        mbedtls
        nvs_flash
)

# Create a NVS image from the contents of the `nvs_data` CSV file
# that fits the partition named 'nvs'. FLASH_IN_PROJECT indicates that
# the generated image should be flashed when the entire project is flashed to
# the target with 'idf.py -p PORT flash'.
nvs_create_partition_image(nvs ../secrets.csv FLASH_IN_PROJECT)
